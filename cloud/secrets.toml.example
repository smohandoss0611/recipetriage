# Paste into Streamlit Advanced settings → Secrets. Use the real private DSN.
# No backend.url, API token, Fireworks key or paid service is needed in this mode.
[app]
mode = "embedded"

[database]
url = "postgresql://USER:PASSWORD@YOUR-POSTGRES-HOST/DBNAME?sslmode=require"
